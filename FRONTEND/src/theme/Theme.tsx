import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#16895E",
    },
    secondary: {
      main: "#891652",
    },
    background: {
      default: "#f7f3db",
    },
  },
});

export default theme;
